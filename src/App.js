import './App.css';
import Home from './pages/Home';

function App() {
  return (
    <div className="app">
      <h1>Let's build a search clone!</h1>
      {/* Home (the one with the search on) */}
      <Home />

      {/* Search page (the results page) */}
    </div>
  );
}

export default App;
